<UserControl x:Class="ModuleProxyOrder.Views.ProxyOrder"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ModuleProxyOrder.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
 

        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="5">
            <Button Content="Neu" Command="{Binding NewProxyCommand}" Margin="0,0,5,0"/>
            <Button Content="Löschen" Command="{Binding DeleteProxyCommand}" Margin="0,0,5,0"/>
            <Button Content="Aktualisieren" Command="{Binding RefreshProxiesCommand}"/>
            <TextBox Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0"/>
        </StackPanel>
        <DockPanel Grid.Row="1" Grid.Column="1" Margin="5">

            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="5">
                <ComboBox Width="150" ItemsSource="{Binding Projects}" SelectedItem="{Binding SelectedTarget}" Margin="0,0,5,0"/>
                <ComboBox Width="150" ItemsSource="{Binding CostUnits}" SelectedItem="{Binding SelectedTarget}" Margin="0,0,5,0"/>
            </StackPanel>
            <Grid DockPanel.Dock="Bottom" Margin="5" HorizontalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5" DataContext="{Binding}">
                    <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Width="200" Text="{Binding SearchText}"/>
                </StackPanel>
            </Grid>
        </DockPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Proxies}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsSynchronizedWithCurrentItem="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding OrderId}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Kostenträger" Binding="{Binding Target}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Beschreibung" Binding="{Binding CommentText}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Erstellt" Binding="{Binding Created}" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
